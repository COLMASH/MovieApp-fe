/** @type {import('next').NextConfig} */
const nextConfig = {
    reactStrictMode: true,
    images: {
        domains: ['m.media-amazon.com', 'ikasnovatv.educacion.navarra.es']
    }
}

module.exports = nextConfig
